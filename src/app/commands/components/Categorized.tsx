import React from 'react'
import { Command } from '../models'
import * as H from '../helpers'

type Props = {
  commands: Command[]
}
export const Categorized = ({ commands }: Props) => {
  return <div></div>
}
