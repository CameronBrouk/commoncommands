import React from 'react'
import styled from 'styled-components'
import { Command } from '../../../models'

interface Props {
  commands: Command[]
}

const SearchableCommands = (props: Props) => {
  return (
    <div>
      {/*  */}
      {/*  */}
      {/*  */}
    </div>
  )
}

export default styled(SearchableCommands)``
